using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MonthlyYield : MonoBehaviour
{ /*
    public int profit; // Monthly profits
    
    private Bank bank;

    private void Start()
    {
        bank = FindObjectOfType<Bank>();

        // Subscribe to the OnBuildingPlaced event of the BuildingCost script
        GetComponent<BuildingCost>().OnBuildingPlaced += OnBuildingPlaced;
    }

    private void OnDestroy()
    {
        // Unsubscribe from the OnBuildingPlaced event when the object is destroyed
        GetComponent<BuildingCost>().OnBuildingPlaced -= OnBuildingPlaced;
    }

    private void OnBuildingPlaced()
    {
        // Update the bank balance and monthly income when the building is placed
        bank.AddIncome(profit);
    }*/
}
