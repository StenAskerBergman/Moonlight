using System.Collections;
using System.Collections.Generic;
using UnityEngine;


// Initialize Resources on the Storage Component
    // When Building on a New Island 
    // When Starting a New game on a island

public class ResourceInitializer : MonoBehaviour
{/*
    public IslandStorage island1;
    public IslandStorage island2;
    public IslandStorage island3;

    public int resource1 = 50;
    public int resource2 = 50;
    public int moneyAmount = 50;

    private void Start()
    {
        island1.AddResource(Enums.Resource.Resource1, resource1);
        island1.AddResource(Enums.Resource.Resource2, resource2);
        island1.AddResource(Enums.Resource.Money, moneyAmount);

        island2.AddResource(Enums.Resource.Resource1, resource1);
        island2.AddResource(Enums.Resource.Resource2, resource2);
        island2.AddResource(Enums.Resource.Money, moneyAmount);

        island3.AddResource(Enums.Resource.Resource1, resource1);
        island3.AddResource(Enums.Resource.Resource2, resource2);
        island3.AddResource(Enums.Resource.Money, moneyAmount);
    }*/
}
