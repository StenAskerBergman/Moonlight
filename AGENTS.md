# Project workflow

For terrain-generation feature work:

- Implement the requested changes, then verify that the project compiles without new errors.
- Do not use TDD or create automated tests unless the user explicitly asks for them.
- Stop after the compile check and let the user run the Unity project for visual and functional validation.
- Do not treat passing automated tests as evidence that generated islands look or behave correctly.
